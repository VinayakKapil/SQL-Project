## Use the same SELECT statement structure to select all records from the “sales” table. Do it twice – once specifying the name of the database explicitly in the SELECT statement, and once, without that specification.

SELECT * FROM sales;

SELECT * FROM sales.sales;